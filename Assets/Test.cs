using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int score;
        score = 150;
        int score2 = 200;
        int score3 = score + score2;
        Debug.Log(score3);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
